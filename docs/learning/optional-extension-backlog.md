# 可选拓展清单

> 本清单记录学习过程中主动提出、但不属于当前主线计划的问题，供 Day1--30 核心主线完成后选择性复习。它不是 [补课台账](learning-gap-ledger.md)：不会阻塞当前 Day，不设置截止日期、验收或必须完成的要求。

## 记录规则

- 仅记录用户主动提出的计划外知识点。
- 当次只直接回答所问问题，不自动追加源码、练习、追问或相邻知识。
- 若与某个 Day 有自然关联，只标注关联 Day；不得改变该 Day 的完成状态或继续位置。
- 只有用户明确要求，或 Day1--30 主线全部完成后，才从本清单选择内容继续学习。

| 主题 | 提问背景 | 简短答案 / 理解锚点 | 关联主线 Day（如有） | 状态 |
|---|---|---|---|---|
| `wait()` 与虚拟线程等待的关系 | 比较 `Object.wait()` 释放锁与虚拟线程调用模型/网络时的等待 | 两者都可能让当前任务暂停，但机制不同：`wait()` 是对象监视器的条件协作，必须释放同一对象锁并等待通知；虚拟线程等待 I/O 时由 JVM 调度，不能替代条件变量或锁协议。 | Day4 | 待后续选择性复习 |
